
************* Module mail
code/main/mail.py:7:0: C0103: Constant name "credentials" doesn't conform to UPPER_CASE naming style (invalid-name)
code/main/mail.py:16:0: C0103: Constant name "receiver" doesn't conform to UPPER_CASE naming style (invalid-name)
code/main/mail.py:17:0: C0103: Constant name "subject" doesn't conform to UPPER_CASE naming style (invalid-name)
code/main/mail.py:18:0: C0103: Constant name "body" doesn't conform to UPPER_CASE naming style (invalid-name)
************* Module rename
code/main/rename.py:1:0: C0114: Missing module docstring (missing-module-docstring)
code/main/rename.py:7:0: W0105: String statement has no effect (pointless-string-statement)
code/main/rename.py:27:0: C0103: Constant name "start_directory" doesn't conform to UPPER_CASE naming style (invalid-name)
code/main/rename.py:5:0: W0611: Unused import shutil (unused-import)
************* Module cleaner
code/utilities/cleaner.py:28:0: C0103: Constant name "start_directory" doesn't conform to UPPER_CASE naming style (invalid-name)
************* Module writing
code/yaml/writing.py:1:0: R0801: Similar lines in 2 files
==Wargame.main:[33:99]
==main:[28:96]
        self.all_sprites = pygame.sprite.Group()
        self.run()

    def run(self):
        '''
            run the game
        '''
        # game loop
        self.playing = True
        while self.playing:
            self.clock.tick(FPS)
            self.events()
            self.update()
            self.draw()

    def update(self):
        '''
            update method
        '''
        self.all_sprites.update()

    def events(self):
        '''
            events method
        '''
        for event in pygame.event.get():
            if event.type == pygame.QUIT:
                if self.playing:
                    self.playing = False
                self.running = False

    def draw(self):
        '''
            render
        '''
        self.screen.fill(BLACK)
        self.all_sprites.draw(self.screen)

        # flip the display always do this last
        pygame.display.flip()

    def show_start_screen(self):
        '''
            start screen
        '''

    def show_go_screen(self):
        '''
            go screen
        '''

def main():
    '''
        main routine
    '''
    g = Game()
    g.show_start_screen()
    while g.running:
        g.new()
        g.show_go_screen()
    pygame.quit()


if __name__ == "__main__":
    # run the main routine
    main() (duplicate-code)
code/yaml/writing.py:1:0: R0801: Similar lines in 2 files
==Wargame.settings:[1:12]
==settings:[4:15]
WIDTH = 360
HEIGHT = 480
FPS = 30
CAPTION = "Template"

# Color Constants
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
RED = (255, 0, 0)
GREEN = (0, 255, 0)
BLUE = (0, 0, 255) (duplicate-code)

-----------------------------------
Your code has been rated at 9.25/10
